# Dependencies
node_modules/

# Build output
dist/
dev-dist/
*.tsbuildinfo

# Test artifacts
test-results/
playwright-report/
coverage/

# Cloudflare Worker local state
.wrangler/

# Internal / agent-workflow docs — not for the public repo
CLAUDE.md
HANDOFF.md
SUITE-PLAN.md
BEAUTY-SPEC.md
cockpit-reference.html
.claude/

# Internal build-log roadmap (root only) — the curated PUBLIC roadmap lives at
# docs/roadmap.md and IS committed. Root-anchored so it doesn't ignore that one.
/ROADMAP.md

# Local env / secrets
.env
.env.*
!.env.example
# .env.production holds only the PUBLIC prod vision-worker URL (no secrets) —
# committed so `npm run build` bakes the right endpoint. Secrets stay in
# .dev.vars (local) / `wrangler secret put` (prod), both still ignored above.
!.env.production

# === Internal working docs — background, benchmarks, deliberation (not public) ===
# The public story lives in README.md · VISION.md · MANIFESTO.md · docs/roadmap.md.
# Everything below is our own working material — best-practice comparisons, coverage
# audits, craft rubrics, decision logs, concept drafts — kept local, never shipped.
/BEST-IN-CLASS-ANALYSIS.md
/COMPONENT-CRAFT.md
/DECISIONS.md
/FOUNDATION-COHERENCE.md
/ICONS.md
/KIT-COVERAGE-AUDIT.md
/POSITIONING.md
/ROLE-CANVAS.md
/SEGMENT-REGISTRY.md
/TABLE-LIST-STICKY-PATTERNS.md
/TAILWIND-APPUI-VARIANT-AUDIT.md
/component-best-practices/

# OS / editor
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/*
!.vscode/extensions.json

# drift-bench generated outputs + fetched kit pack (harness + results.json stay)
bench/runs/
bench/kit/

# Personal working artifact (the mobile-configurator study, exported as a blog
# post) — kept locally, never part of the public repo.
/mobile-configurator-study.article.html
