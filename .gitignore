# Dependencies
node_modules/

# Build output
dist/
dev-dist/
*.tsbuildinfo

# Test artifacts
test-results/
playwright-report/
coverage/

# Cloudflare Worker local state
.wrangler/

# Internal / agent-workflow docs — not for the public repo
CLAUDE.md
HANDOFF.md
SUITE-PLAN.md
BEAUTY-SPEC.md
cockpit-reference.html
.claude/

# Internal build-log roadmap — a curated public ROADMAP replaces this
ROADMAP.md

# Local env / secrets
.env
.env.*
!.env.example
# .env.production holds only the PUBLIC prod vision-worker URL (no secrets) —
# committed so `npm run build` bakes the right endpoint. Secrets stay in
# .dev.vars (local) / `wrangler secret put` (prod), both still ignored above.
!.env.production

# OS / editor
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/*
!.vscode/extensions.json
